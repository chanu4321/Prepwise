[pytest]
testpaths = backend/tests
pythonpath = backend
addopts = -q
# Tests point Qdrant at an unreachable address on purpose; the client's background version check then warns
filterwarnings =
    ignore:Failed to obtain server version:UserWarning
